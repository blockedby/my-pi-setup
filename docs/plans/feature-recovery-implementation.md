# Feature recovery implementation — working ledger

Status: implementation in progress, not accepted or installed. Initial audit blockers AUD-001/AUD-002/AUD-003 are independently closed; remaining-scope continuation is authorized. See `feature-recovery-audit-remediation.md` for evidence. User approved finishing the twelve changes illustrated in `docs/feature-pipeline-refactor.ru.html`, using Astra agents. Preserve unrelated dirty 0.86.1 upgrade files. No commits, push, installation, live-provider runs, or active-session restart authorized in this implementation step.

## Frozen direction

- Keep feature graph, series-parallel compiler, fork/join, independent Luna audit.
- All feature coding roles use Astra/low (already implemented and focused-tested before this refactor).
- A task owns one persistent execution session and an append-only local commit range.
- Commands return evidence; acceptance is separate from staging, checkpointing and running checks.
- Successful commands may generate tracked files. Record input/output contents; do not require a generator to converge to zero diff. Later meaningful edits invalidate evidence conservatively.
- Preparation/baseline failures reach the executor as repair context, rather than preventing its launch.
- Use host-mediated scoped stage/checkpoint tools; keep shared Git metadata hidden. No arbitrary push/refs/history rewriting.
- Ordinary checks remain offline; explicit preparation gets isolated environment/network capability. Do not describe unrestricted shared networking as registry-only access.
- Independent branches continue after local task failure; dependents remain blocked. Cancellation and integrity failures retain authority.
- Preserve pipeline_name, mandatory plan_path and wallclock_limit. API changes and old reports require explicit compatibility handling.

## File ownership / waves

- Parent: `feature-execution-contract.ts` (added shared request/range types), controller/controller tests, domain/index/API, prompts, remaining consumers, evidence/dashboard, integration tests and public docs/log.
- Completed sa-4: `session.test.ts` Astra fixture update; 8 offline tests reported passing. Subsequent session owner may change it after integration.
- Completed sa-5: `feature-task-worktrees.ts` and test. Added stage/checkpoint/range/assertCleanHandoff and lifecycle verifyOwnership. Legacy methods retained. 27 focused tests reported passing. Checkpoint provenance currently in memory; parent must link accepted range evidence before cleanup.
- Completed sa-6 and sa-11: task-runtime and check-input-revision plus tests, including supplementary check IDs and conservative obligation preservation; worker follow-up reports 44 tests passing.
- Completed sa-7 and sa-12: feature-sandbox and tests; ordinary-shell broad host-read exposure removed; latest worker reports 26 tests passing. Preparation network egress remains unrestricted and must not be described as registry-only; home-managed toolchains need explicit provisioning.
- Completed sa-8 and parent integration: diagnostic readiness provenance and optional excerpts wired through session/controller, optional acceptanceRefs validated, safe narrative defaults applied before SDK validation.
- Completed sa-9: graph-executor and tests; local failure isolation, recovery continuations, commit ranges and joins; 47 tests reported passing. No automatic planner intervention. Parent subsequently added root-check reference validation in feature-graph and its tests.
- Completed sa-10/sa-13 and parent integration: recovery tools exposed, narrative normalization integrated, supplementary dispatch wired and tested.
- Completed sa-14: prompt.ts teaches the new recovery contracts; no literal prompt assertions added.
- Completed sa-15: feature-execution-integration.test.ts; migrated to explicit checkpoints/acceptance with two commits per task, twelve preserved commits through nested joins, generated-output evidence and exact content assertions. Observed ~6.8s fixture now has bounded 20s timeout rather than default 5s. Worker reports 107 tests passing in five-file run; parent reviewed diff and launched an independent rerun plus full repository checks.

## Parent integration and remaining evidence

- `runFeatureTaskSession` caches persistent session IDs by role and sends subsequent repair turns into the same tree session; host bindings remain available for session lifetime. Need cancellation/authority close and actual session-reuse tests.
- Removed immediate failRun after readiness command failure and exact readiness-command match gate for graph baseline.
- Discovery with no readiness observations persists an explicit empty readiness artifact instead of inventing success. Cleanup warnings become diagnostic events.
- Integrated diagnostic provenance and explicit execution observations into controller readiness results. Nine controller readiness/baseline regressions passed in the parent run.
- Final-review proxy exposes preparation/staging/checkpoints and checks active review authority. Bounded same-session final-review continuation now passes real-Git controller recovery regression, including one worker prompt plus one continuation send per task. Parent controller/graph selection: 76 tests passed; typecheck and lint passed.
- Accepted task snapshots are synchronously written/fsynced before graph callbacks return; persistence failure aborts graph execution. Full durability, cancellation and join-cleanup ordering still require tests.

## Remaining integration risks / acceptance

1. Multi-checkpoint ranges must survive nested joins, active cherry-pick repair and empty environment-only repair without replay/loss.
2. Generated-output acceptance must preserve input freshness; no `echo ok` string heuristic claims semantic coverage.
3. Failure before preparation completes must permit executor-owned repair and refreshed recipe evidence.
4. Task session persistence must be actual SDK reuse, not just stable synthetic IDs. Technical session loss and terminal authority need explicit handling.
5. New tool schemas must match host capabilities; final review proxy must expose every supported method.
6. Graph failure isolation must include starting later independent tasks, not only letting in-flight siblings finish.
7. No inherited dirty exemptions in new handoff. Never delete unrelated files or credentials.
8. Public API simplification, initial workspace preparation policy, safe remaining-graph intervention and report compatibility are not complete merely because modules compile.
9. Persist accepted commit ranges/check evidence before claiming durable acceptance or deleting child worktrees.
10. Required final verification: targeted suites, real Git/bubblewrap offline integration, cancellation/evidence regressions, repository check/format/lint; audit/review before final acceptance. Existing runtime remains unchanged until separate rollout.

## Continuation after blocker closure

Astra investigation sa-24 and implementation scopes sa-27/28/29/30 completed; parent integrated technical-session replacement/revocation. Completed sa-25 established that executable planner intervention requires a nonterminal suspension and preserved continuation; simply rerunning the tree would replay accepted work. Completed sa-26 recommends workspace-local provisioning rather than expanding host mounts. Parent freshly ran three preparation/isolation/cancellation tests successfully. Optional home-managed tool snapshots and actual vp compatibility remain outside verified support. Parent retains shared contracts, API policy and wiring. Existing audit closure is not being reopened.

Session recovery verification: parent integrated actual tree-assigned IDs at factory admission, typed missing/disposed availability, one replacement on the next existing continuation, same runtime/checkpoint retention, session-bound native and host-tool abort/fencing, sticky bounded teardown failure and terminal task revocation. Fixed integration regressions rather than weakening old cancellation behavior: a successful disposal settles cancellation even if interrupt rejected, while failed disposal never proves quiescence. Fresh `bt-22`: 97 controller/session/tree tests passed, followed by check/format/lint/diff; terminal runtime regression separately passed and worker runtime suite reported 54 passed before the added parent test. Independent session-loss review and closure completed: `session-admission-counterproof-closure-0a3a6768` has no findings, both session findings closed, implementation acceptance passed. Executor verified 700 Bun + 22 Vitest tests and full static checks. Construction-failure candidate-authority claim was disproved; drain test timeout was corrected without changing the production deadline. This is not crash-resume or remaining-DAG replanning.

Executable graph-patch boundary (not implemented or authorized): changing the remaining graph would require active branches to drain; preserve completed/started identities, obligations, ranges and ownership. The implemented read-only advice path does not change that graph and may run while independent branches continue. A patch to an unstarted descendant cannot unblock a failed prerequisite. Do not mistake a recorded plan suggestion at the terminal boundary for executable resumption. Arbitrary replacement DAGs and crash-resume are not authorized by the proposal.

Scope clarification from the approved HTML: explicit pipeline discriminator, audit-input simplification and hiding worktree layout are labeled **still under discussion**, not finalized contracts. Do not silently introduce breaking public API changes or relax caller-prepared-worktree policy while implementing recovery. Establish whether workspace-local preparation already satisfies provisioning before adding host mounts.

## Final pending implementation pass

User requested finishing pending tasks. Implementing the approved local-repair → planner-consultation path with bounded advice to the existing runtime, not a replacement DAG. Planner consultation is serialized in the existing read-only canonical session; independent branches continue, and the controller persists request/decision before resuming. Advice cannot change topology, checks, requirements, checkpoints or acceptance. At most one consultation per runtime within its existing total attempt budget. Explicitly additional arbitrary decomposition edits remain outside this bounded path.

Parent owns the strict decision contract and controller wiring/integration tests. Astra owns graph runtime/tests and separate decision-schema tests; another read-only scope maps all twelve main proposals against existing evidence. Fresh integrated verification and scoped audit are pending. No commits, install or runtime restart.

This ledger records work allocation, not proof of completion. Agent reports require parent review and fresh integrated verification.

## Twelve-item acceptance map (source implementation accepted)

| Proposal                               | Implementation and evidence                                                                                                                                                                                                                                                                                                                                                                                             |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01 Preparation recovery                | Runtime validates exact inherited recipe replacements before execution; only successful proposals from accepted tasks propagate through branch lineage. Four graph tests cover order, caller immutability, failed proposals and sibling isolation.                                                                                                                                                                      |
| 02 Generated outputs                   | Runtime separates command evidence, dirty source and acceptance; freshness does not require generator fixed-point. Original independently closed suite retains generated-output regressions.                                                                                                                                                                                                                            |
| 03 Diagnostic readiness                | Readiness records facts without preventing executor launch for ordinary recipe/provenance failures. Security and persistence gates remain authoritative.                                                                                                                                                                                                                                                                |
| 04 Checkpoint commits                  | Host checkpoint tools preserve append-only accepted commit ranges and persist task snapshots before returning. Real-Git graph/session recovery regressions retain ranges.                                                                                                                                                                                                                                               |
| 05 Shared staging                      | Stage/checkpoint/acceptance tools provide the primary path; legacy finalization remains an adapter and must receive equivalent handoff handling.                                                                                                                                                                                                                                                                        |
| 06 Clean owned handoff                 | Lifecycle retains dirty diagnostics/cache and recreates an owned child at accepted HEAD. Graph refreshes paths/targets, clears inherited residual paths and reruns preparation/baseline. Caller-owned root is never implicitly replaced; legacy root fingerprint safeguards remain. Lifecycle suite: 41 tests. New integrated regression caught and corrected missing legacy-commit triggering and clean-gate ordering. |
| 07 Behavioral checks                   | Supplementary/revised checks remain linked to requirements without deleting immutable original check obligations. Prior AUD-002 independently closed.                                                                                                                                                                                                                                                                   |
| 08 Persistent sessions                 | Same task session for ordinary failures; objectively unavailable sessions replaced within the existing budget with revoked/drained authority. Independent session closure passed 700 Bun + 22 Vitest tests.                                                                                                                                                                                                             |
| 09 Independent branches and escalation | Trusted local failures isolate subtrees. At most one bounded read-only planner consultation per stalled runtime, within the 24-attempt cap; persisted advice cannot mutate topology or grant acceptance. Retry, blocked and invalid controller scenarios pass.                                                                                                                                                          |
| 10 Join repair                         | Fork/join, conflict continuation, nested source-to-integrated provenance and environment-only repair retained. Graph integration suite remained green before the additional owned-handoff regression.                                                                                                                                                                                                                   |
| 11 Workspace tools and isolation       | Ordinary tools are lease-fenced; workspace-local online preparation and offline implementation remain isolated from host credentials/home. Three fresh sandbox provisioning/isolation/cancellation tests passed earlier. Home-managed vp is not verified support.                                                                                                                                                       |
| 12 Narrative tolerance                 | Defaults plus deterministic exact deduplication/sorting of only repositoryConventions/relevantDiscovery. Original limits checked before reduction; control fields and ordered instructions untouched. Native submission normalization and strict decision-schema coverage retained.                                                                                                                                     |

Final acceptance evidence: parent `bt-10` and independent closure executor both passed `bun run test` (805 Bun + 22 Vitest tests), `bun run check`, `bun run format:check`, `bun run lint` and `git diff --check`. Targeted suites include 55 graph, 41 lifecycle and 70 controller tests. Initial remaining-slice audit `verified-recovery-slice-audit-017a6a3b` identified one planner fatal-authority blocker; remediation and independent closure `planner-fatal-authority-closure-1c99ff05` closed AUD-001 with `findings: []`, `unprovenChecks: []` and passed implementation acceptance. Original residual and session-loss closures remain recorded above. All twelve approved source implementation items are accepted within the documented ownership/API boundaries.

The closure's pipeline-execution envelope remains unproven solely because the read-only audit supplied no cleanup intent/outcome events; this is not a confirmed product defect or a claim of cleanup success. No runtime installation, restart, live-provider test or deployment occurred. Provider-side narrative omission handling and actual home-managed vp compatibility remain unverified; no such support is promised. Discussion-only breaking API changes, arbitrary graph rewriting and crash-resume remain outside scope.
